[package]
name = "type_expr_compiler"
version = "0.1.0"
edition = "2024"
description = "A typed struct/match/enum parser and compiler to JSON"
license = "MIT OR Apache-2.0"

[lib]

[dependencies]
chumsky = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
